%% 2015-09-24 - RSVP analysis within subjects
% Skeleton script for basic, within-subject data analysis for RSVP. 

function RSVP_within_subjects(Plots, version)

% Create number string from the date (convertible with 'datestr' function)
date_string = mat2str(datenum(date));

% Specify paths
across_path = '../../data/across_subjects/';
figure_path = '../../figures/';
results_path = '../../results/';

end